name: jeetendra_portfolio
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.1
  responsive_builder: ^0.7.1
  firebase_auth: ^6.1.4
  firebase_core: ^4.4.0
  flutter_riverpod: ^2.6.1
  cloud_firestore: ^6.1.2
  cached_network_image: ^3.4.1
  image_picker: ^1.2.1
  firebase_storage: ^13.0.6
  intl: ^0.20.2
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/